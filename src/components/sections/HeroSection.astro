---
import { siteContent } from '../../core/data/content';
import PremiumButton from '../ui/PremiumButton.astro';
const base = import.meta.env.BASE_URL;
---

<section id="hero" class="relative min-h-screen flex items-end overflow-hidden pb-24 md:pb-32 bg-[var(--color-bg-primary)]">
  <!-- Cinematic Background Layers -->
  <div class="hero-camera absolute inset-0 z-0">
    <div class="parallax-bg absolute inset-0 scale-110 origin-center blur-[16px]">
      <img 
        src="${base}images/hero.png" 
        alt="Interior de Nebula Glitch Social Club — Cafetería de especialidad"
        class="w-full h-full object-cover object-center"
        loading="eager"
        decoding="async"
      />
      <!-- Gradient overlays for readability and atmosphere -->
      <div class="hero-fade-layer absolute inset-0 bg-gradient-to-t from-[var(--color-bg-primary)] via-[var(--color-bg-primary)]/70 to-transparent mix-blend-multiply"></div>
      <div class="hero-fade-layer absolute inset-0 bg-gradient-to-r from-[var(--color-bg-primary)]/90 to-transparent"></div>
    </div>
  </div>

  <!-- Interactive Atmospheric Glow -->
  <div id="interactive-glow" class="hero-atmosphere absolute top-0 left-0 w-[60vw] h-[60vw] md:w-[40vw] md:h-[40vw] rounded-full bg-[var(--color-glow-primary)] blur-[120px] opacity-20 mix-blend-screen z-[1] -translate-x-1/2 -translate-y-1/2 hidden md:block"></div>
  <!-- Mobile Static Glow -->
  <div class="hero-atmosphere absolute top-[-20%] right-[-10%] w-[60%] h-[60%] rounded-full bg-[var(--color-glow-primary)] blur-[100px] opacity-15 mix-blend-screen z-[1] md:hidden"></div>

  <!-- Content — anchored to the bottom-left for editorial composition -->
  <div class="hero-content relative z-10 w-full max-w-7xl mx-auto px-6 md:px-12 pointer-events-none">
    <div class="max-w-3xl flex flex-col gap-6">
      <h1 class="hero-title text-6xl md:text-8xl lg:text-9xl font-bold tracking-tighter text-[var(--color-text-primary)] leading-[0.9] blur-[8px]">
        {siteContent.hero.title}
      </h1>
      <h2 class="hero-subtitle text-lg md:text-2xl font-light tracking-[0.25em] text-[var(--color-accent-primary)] overflow-hidden">
        <span class="block translate-y-full opacity-0">{siteContent.hero.subtitle}</span>
      </h2>
      <p class="hero-tagline text-xl md:text-2xl text-[var(--color-text-secondary)] font-secondary font-light mt-2 overflow-hidden">
        <span class="block translate-y-full opacity-0">{siteContent.hero.tagline}</span>
      </p>
      <div class="hero-actions flex flex-wrap gap-4 mt-4 opacity-0 pointer-events-auto">
        <PremiumButton href="#philosophy">
          {siteContent.hero.cta}
        </PremiumButton>
        <PremiumButton href="#menu" class="border-transparent text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)]">
          {siteContent.hero.ctaMenu}
        </PremiumButton>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="hero-scroll absolute bottom-12 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 opacity-0 z-10">
    <span class="text-[10px] uppercase tracking-widest text-[var(--color-text-secondary)]">Scroll</span>
    <div class="w-[1px] h-12 bg-gradient-to-b from-[var(--color-text-secondary)] to-transparent"></div>
  </div>
</section>

<script>
  import gsap from 'gsap';
  import ScrollTrigger from 'gsap/ScrollTrigger';
  import { EASINGS, DURATIONS } from '../../core/motion/tokens';
  import { TextDecoder } from '../../core/motion/TextDecoder';

  const initHeroAnimation = () => {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const title = document.querySelector('.hero-title');
    
    if (prefersReducedMotion) {
      gsap.set('.hero-subtitle span, .hero-tagline span', { y: 0, opacity: 1 });
      gsap.set('.hero-actions, .hero-scroll', { opacity: 1 });
      gsap.set('.parallax-bg', { filter: 'blur(0px)' });
      gsap.set(title, { filter: 'blur(0px)', opacity: 1 });
      return;
    }

    if (!title) return;

    gsap.set(title, { opacity: 0 });

    const decoder = new TextDecoder(title as HTMLElement);
    const tl = gsap.timeline({ defaults: { ease: EASINGS.premiumEntrance } });

    // Cinematic Reveal (Focus Pull)
    tl.to('.parallax-bg', {
      scale: 1,
      filter: 'blur(0px)',
      duration: 2.5,
      ease: "power2.out"
    })
    .to(title, {
      opacity: 1,
      filter: 'blur(0px)',
      duration: 0.1,
      onStart: () => {
        decoder.decode(1.5, 0);
      }
    }, 0.3)
    .to('.hero-subtitle span', {
      y: '0%',
      opacity: 1,
      duration: 1,
      ease: "power3.out"
    }, 1.2)
    .to('.hero-tagline span', {
      y: '0%',
      opacity: 1,
      duration: 1,
      ease: "power3.out"
    }, 1.4)
    .to('.hero-actions', {
      opacity: 1,
      duration: 1,
    }, 1.6)
    .to('.hero-scroll', {
      opacity: 1,
      duration: DURATIONS.entrance,
    }, 1.8);

    // Breathing Engine (Perpetual floating)
    gsap.to('.hero-camera', {
      y: 15,
      rotationZ: 0.5,
      duration: 20,
      ease: "sine.inOut",
      yoyo: true,
      repeat: -1
    });

    gsap.to('.hero-content', {
      y: -10,
      rotationZ: -0.5,
      duration: 15,
      ease: "sine.inOut",
      yoyo: true,
      repeat: -1
    });

    // Choreography Parallax on Scroll
    const scrollTl = gsap.timeline({
      scrollTrigger: {
        trigger: '#hero',
        start: 'top top',
        end: 'bottom top',
        scrub: true
      }
    });

    scrollTl.to('.hero-content', {
      y: '-20vh',
      filter: 'blur(10px)',
      opacity: 0,
      ease: 'none'
    }, 0)
    .to('.hero-atmosphere', {
      y: '-30vh',
      ease: 'none'
    }, 0)
    .to('.hero-camera', {
      y: '15vh',
      ease: 'none'
    }, 0)
    .to('.hero-fade-layer', {
      backgroundColor: 'var(--color-bg-primary)',
      opacity: 1,
      ease: 'none'
    }, 0);

    // Interactive Glow
    const glow = document.getElementById('interactive-glow');
    if (glow && !matchMedia('(hover: none)').matches) {
      const xSet = gsap.quickSetter(glow, "x", "px");
      const ySet = gsap.quickSetter(glow, "y", "px");
      
      let mouseX = window.innerWidth / 2;
      let mouseY = window.innerHeight / 2;
      let currX = mouseX;
      let currY = mouseY;
      
      window.addEventListener('mousemove', (e) => {
        mouseX = e.clientX;
        mouseY = e.clientY;
      });

      gsap.ticker.add(() => {
        const dt = 1.0 - Math.pow(1.0 - 0.05, gsap.ticker.deltaRatio());
        currX += (mouseX - currX) * dt;
        currY += (mouseY - currY) * dt;
        xSet(currX);
        ySet(currY);
      });
    }
  };

  document.addEventListener('astro:page-load', initHeroAnimation);
</script>
